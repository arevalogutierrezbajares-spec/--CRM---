"use client";

/**
 * @deprecated Center floating dock removed — use BrainSearch in the rail.
 * Kept as a no-op so any stray import does not reintroduce center search.
 */
export function RebuildGuardCenter() {
  return null;
}
