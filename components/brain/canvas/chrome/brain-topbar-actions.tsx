"use client";

import { BrainSearch } from "./brain-search";

/** TopBar action slot — focuses rail search (or opens mobile focus path). */
export function BrainTopBarActions() {
  return <BrainSearch variant="topbar" />;
}
